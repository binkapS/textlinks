name: flutter_textlinks_app
description: Textlinks is Text package that detects URLs and Emails in a String and when tapped performs a specified action.
publish_to: 'none'
homepage: https://github.com/binkapS/textlinks

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_textlinks:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

